apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: retail
  labels:
    app.kubernetes.io/name: orders
    app.kubernetes.io/part-of: retail-store
    app.kubernetes.io/component: application
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: orders
  template:
    metadata:
      labels:
        app.kubernetes.io/name: orders
        app.kubernetes.io/part-of: retail-store
        app.kubernetes.io/component: application
    spec:
      serviceAccount: orders
      containers:
      - name: orders
        image: 147723036683.dkr.ecr.us-east-1.amazonaws.com/retail-orders
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            cpu: "150Mi"
            memory: "500m"
        ports:
        - name: http
          containerPort: 8080
